<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Para Dana ❤️</title>
  <style>
    body {
      margin: 0;
      background: black;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      overflow: hidden;
    }
    svg {
      width: 80vw;
      max-width: 400px;
    }
    .heart-path {
      fill: none;
      stroke: red;
      stroke-width: 4;
      stroke-dasharray: 1000;
      stroke-dashoffset: 1000;
      animation: drawHeart 3s ease-in-out forwards, pulse 2s infinite ease-in-out 3s;
    }
    .infinity {
      fill: none;
      stroke: red;
      stroke-width: 3;
      stroke-dasharray: 300;
      stroke-dashoffset: 300;
      animation: drawInfinity 2s ease-in-out forwards 1.5s;
    }
    @keyframes drawHeart {
      to { stroke-dashoffset: 0; }
    }
    @keyframes drawInfinity {
      to { stroke-dashoffset: 0; }
    }
    @keyframes pulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.05); }
    }
  </style>
</head>
<body>
  <svg viewBox="0 0 200 180">
    <path class="heart-path" d="M100 170
      C100 170 10 100 10 50
      C10 10 60 10 100 50
      C140 10 190 10 190 50
      C190 100 100 170 100 170 Z" />
    <path class="infinity" d="M50 90
      C30 70, 30 110, 50 90
      C70 70, 90 70, 100 90
      C110 110, 130 110, 150 90
      C170 70, 170 110, 150 90
      C130 70, 110 70, 100 90
      C90 110, 70 110, 50 90" />
  </svg>
</body>
  </html>
